# The shared Thalamus Convex deployment (auth + AO credits live there).
# The public agent API is derived from this at runtime:
#   https://<deployment>.convex.site/ao/v1/*
VITE_CONVEX_URL=https://your-deployment.convex.cloud
